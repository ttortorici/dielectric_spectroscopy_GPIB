This repo is for doing commanding dielectric spectroscopy instruments through GPIB

install labjcak prereqs https://labjack.com/support/software/installers/ljm
https://labjack.com/support/software/installers/ud

Instructions to set up conda environment:
in conda command prompt (windows) or terminal (unix)
    conda create --name dielectric
    conda activate dielectric
    conda install git
    conda install numpy
    conda install scipy
    pip install pyqt5
    pip install pyvisa
    conda install -c conda-forge pyqtgraph
    #  conda install -c conda-forge jupyterlab
    pip install PyYaml
    pip insatll pyserial
    pip install labjack-ljm

clone this repository https://github.com/labjack/LabJackPython.git
    and then run:
	python setup.py install


https://pythonspot.com/pyqt5-absolute-position/